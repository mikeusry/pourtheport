---
const klaviyoApiKey = import.meta.env.PUBLIC_KLAVIYO_API_KEY;
const productId = import.meta.env.PUBLIC_SHOPIFY_PRODUCT_ID || '9943643023';
const isProduction = import.meta.env.PROD;

// Only load Klaviyo if API key is properly configured (should start with 'pk_' and be at least 20 chars)
const enableKlaviyo = klaviyoApiKey &&
  klaviyoApiKey !== 'your-klaviyo-api-key-here' &&
  klaviyoApiKey.startsWith('pk_') &&
  klaviyoApiKey.length > 20;
---

{enableKlaviyo && (
  <>
    <!-- Klaviyo.js Script - Deferred Loading -->
    <script type="text/javascript">
      // Load Klaviyo after user interaction or after 4 seconds
      let klaviyoLoaded = false;
      function loadKlaviyo() {
        if (klaviyoLoaded) return;
        klaviyoLoaded = true;
        
        const script = document.createElement('script');
        script.type = 'text/javascript';
        script.async = true;
        script.src = `https://static.klaviyo.com/onsite/js/${klaviyoApiKey}/klaviyo.js`;
        document.head.appendChild(script);
        console.log('📧 Klaviyo reviews loaded');
      }
      
      // Load on scroll to reviews section or after timeout
      const observer = new IntersectionObserver((entries) => {
        if (entries[0].isIntersecting) {
          loadKlaviyo();
          observer.disconnect();
        }
      });
      
      document.addEventListener('DOMContentLoaded', () => {
        const reviewsContainer = document.querySelector('.klaviyo-reviews-container');
        if (reviewsContainer) observer.observe(reviewsContainer);
      });
      
      // Fallback: load after 4 seconds
      setTimeout(loadKlaviyo, 4000);
    </script>
    
    <!-- Reviews Container -->
    <div class="klaviyo-reviews-container">
      <!-- Star Rating Widget -->
      <div class="klaviyo-star-rating-widget" 
           data-id={productId} 
           data-product-title="Pour the PORT Septic Treatment" 
           data-product-type="septic_maintenance">
      </div>
      
      <!-- Reviews Summary and List Combined -->
      <div id="klaviyo-reviews-all" data-id={productId}></div>
    </div>
  </>
)}

{!enableKlaviyo && (
  <div class="reviews-container bg-gradient-to-br from-white to-brand-background-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden">
    <!-- Header Section -->
    <div class="bg-brand-primary-green text-white px-8 py-6">
      <div class="text-center">
        <div class="flex justify-center items-center mb-4">
          <div class="flex space-x-1">
            {[...Array(5)].map(() => (
              <svg class="w-7 h-7 text-brand-accent-orange drop-shadow-sm" fill="currentColor" viewBox="0 0 20 20">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"></path>
              </svg>
            ))}
          </div>
          <span class="ml-3 text-2xl font-bold">4.8</span>
        </div>
        <h3 class="text-3xl font-bold mb-2">Trusted by Customers</h3>
        <p class="text-green-100 text-lg font-medium">Over 200+ verified reviews</p>
      </div>
    </div>

    <!-- Reviews Grid -->
    <div class="p-8">
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <!-- Review 1 -->
        <div class="bg-white rounded-xl shadow-md hover:shadow-lg transition-all duration-300 p-6 border border-gray-100">
          <div class="flex items-center justify-between mb-4">
            <div class="flex space-x-1">
              {[...Array(5)].map(() => (
                <svg class="w-5 h-5 text-brand-accent-orange" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"></path>
                </svg>
              ))}
            </div>
            <div class="w-2 h-2 bg-brand-secondary-blue rounded-full"></div>
          </div>
          <div class="mb-4">
            <h4 class="font-bold text-brand-neutral-dark text-lg mb-1">Sarah M.</h4>
            <span class="text-brand-secondary-blue text-sm font-medium bg-brand-secondary-blue/10 px-2 py-1 rounded-full">Verified Purchase</span>
          </div>
          <blockquote class="text-gray-700 leading-relaxed font-medium italic">
            "Been using Pour the PORT for 2 years now. No septic issues whatsoever! So much better than the expensive pumping we used to do every year."
          </blockquote>
        </div>

        <!-- Review 2 -->
        <div class="bg-white rounded-xl shadow-md hover:shadow-lg transition-all duration-300 p-6 border border-gray-100">
          <div class="flex items-center justify-between mb-4">
            <div class="flex space-x-1">
              {[...Array(5)].map(() => (
                <svg class="w-5 h-5 text-brand-accent-orange" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"></path>
                </svg>
              ))}
            </div>
            <div class="w-2 h-2 bg-brand-secondary-blue rounded-full"></div>
          </div>
          <div class="mb-4">
            <h4 class="font-bold text-brand-neutral-dark text-lg mb-1">Mike T.</h4>
            <span class="text-brand-secondary-blue text-sm font-medium bg-brand-secondary-blue/10 px-2 py-1 rounded-full">Verified Purchase</span>
          </div>
          <blockquote class="text-gray-700 leading-relaxed font-medium italic">
            "Works exactly as promised. Easy to use, eco-friendly, and saves thousands in pumping costs. If you have a septic system, this is a must-have."
          </blockquote>
        </div>

        <!-- Review 3 -->
        <div class="bg-white rounded-xl shadow-md hover:shadow-lg transition-all duration-300 p-6 border border-gray-100">
          <div class="flex items-center justify-between mb-4">
            <div class="flex space-x-1">
              {[...Array(5)].map(() => (
                <svg class="w-5 h-5 text-brand-accent-orange" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"></path>
                </svg>
              ))}
            </div>
            <div class="w-2 h-2 bg-brand-secondary-blue rounded-full"></div>
          </div>
          <div class="mb-4">
            <h4 class="font-bold text-brand-neutral-dark text-lg mb-1">Jennifer R.</h4>
            <span class="text-brand-secondary-blue text-sm font-medium bg-brand-secondary-blue/10 px-2 py-1 rounded-full">Verified Purchase</span>
          </div>
          <blockquote class="text-gray-700 leading-relaxed font-medium italic">
            "My septic company recommended this after our last pumping. 18 months later and everything is running perfectly. Natural and effective!"
          </blockquote>
        </div>
      </div>

      <!-- Bottom CTA -->
      <div class="mt-8 text-center bg-gradient-to-r from-brand-primary-green to-brand-secondary-blue rounded-xl p-6 text-white">
        <p class="text-lg font-semibold mb-2">Join Over 200 Happy Customers</p>
        <p class="text-green-100 font-medium">Protecting septic systems nationwide since 2018</p>
      </div>
    </div>
  </div>
)}

<script define:vars={{ klaviyoApiKey, isProduction }}>
document.addEventListener('DOMContentLoaded', function() {
  if (!klaviyoApiKey || klaviyoApiKey === 'your-klaviyo-api-key-here') {
    if (!isProduction) {
      console.log('🔍 Klaviyo Reviews: Disabled - API key not configured');
    }
    return;
  }

  // Wait for Klaviyo to load
  let klaviyoRetries = 0;
  const maxRetries = 10;
  
  function waitForKlaviyo() {
    if (typeof window.klaviyo !== 'undefined') {
      // Klaviyo is loaded, initialize reviews
      if (!isProduction) {
        console.log('📊 Klaviyo Reviews: Initialized successfully');
      }
      
      // Track reviews interactions for GA4
      if (typeof gtag !== 'undefined') {
        // Listen for review widget interactions
        document.addEventListener('click', function(e) {
          if (e.target.closest('.klaviyo-star-rating-widget') || 
              e.target.closest('#klaviyo-reviews-all')) {
            gtag('event', 'klaviyo_review_interaction', {
              event_category: 'engagement',
              event_label: 'review_widget_click'
            });
          }
        });
      }
      
    } else if (klaviyoRetries < maxRetries) {
      klaviyoRetries++;
      setTimeout(waitForKlaviyo, 500);
    } else {
      if (!isProduction) {
        console.warn('⚠️ Klaviyo Reviews: Failed to load after 5 seconds');
      }
    }
  }
  
  // Start checking for Klaviyo
  waitForKlaviyo();
});
</script>

<style>
.klaviyo-reviews-container {
  max-width: 100%;
  margin: 0 auto;
}

/* Star Rating Widget Styling */
.klaviyo-star-rating-widget {
  margin-bottom: 1.5rem;
  text-align: center;
}

/* Reviews Widget Styling */
#klaviyo-reviews-all {
  min-height: 200px;
}

/* Fallback styling */
.klaviyo-fallback {
  min-height: 200px;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Responsive adjustments */
@media (max-width: 768px) {
  .klaviyo-reviews-container {
    padding: 0 1rem;
  }
}

/* Custom styling for Klaviyo widgets (when they load) */
:global(.klaviyo-reviews-all) {
  font-family: inherit;
}

:global(.klaviyo-star-rating-widget) {
  font-family: inherit;
}

/* Loading state */
.klaviyo-reviews-container:empty::after {
  content: "Loading reviews...";
  display: block;
  text-align: center;
  padding: 2rem;
  color: #666;
  font-style: italic;
}
</style>